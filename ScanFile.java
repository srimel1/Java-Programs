import java.io.FileNotFoundException;
import java.io.IOException;
import java.io.PrintWriter;
import java.nio.file.Files;
import java.nio.file.Path;
import java.nio.file.Paths;
import java.util.HashMap;
import java.util.HashSet;
import java.util.Scanner;
import java.util.StringTokenizer;
import java.util.stream.Stream;

//public class ScanFile {
/*
	public static void main(String[] args) throws FileNotFoundException {
		int count = 0;
		HashMap<String, Integer> map = new HashMap();
		HashSet<String> set = new HashSet();
		String strObj = null;
		StringTokenizer st = new StringTokenizer(strObj);
		String s;
		HashMap<String, Integer> counts = new HashMap<String, Integer>();
		while (st.hasMoreTokens()) {
		     s=st.nextToken();
		    
		    
			if(map.containsKey(s))
		    {
		        count=(Integer)map.get(s);
		        count++;
		        map.put(s,count);
		        set.add(s);
		    }
		    else
		    {
		        map.put(s,count);
		        set.add(s);
		    }
		
		//tokens[] = scanner.tokenize(line, '<space>');
		//int c = counts.get(eachWord);
		
		PrintWriter toFile = new PrintWriter("out.txt") ;
		try (Stream<Path> filePathStream=Files.walk(Paths.get("C:\\Users\\snowe\\eclipse-workspace\\DataStructures\\src"))) {
		    filePathStream.forEach(filePath -> {
		        if (Files.isRegularFile(filePath)) {
		            System.out.println(filePath);
		            //toFile.print(filePath) ;
		        }
		    });
		    
		    String words;
		    int j;
		    
		} catch (Exception e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		
	}
*/
	

	